VITE_API_GATEWAY_URL=http://localhost:8002

# vehicle_id que debe coincidir con el perfil dummy de la app movil (el
# primer perfil de dummyVehicleProfiles.ts) — actualizalo si cambias el
# vehicle_id en el campo de texto de la app movil (mobile/App.tsx).
VITE_MOBILE_VEHICLE_ID=veh-mobile-1
